import { MovimientoInventario } from './movimiento-inventario';

describe('MovimientoInventario', () => {
  it('should create an instance', () => {
    expect(new MovimientoInventario()).toBeTruthy();
  });
});
